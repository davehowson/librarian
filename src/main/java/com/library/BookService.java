package com.library;

import com.library.model.Book;

import java.util.List;

public class BookService {

//    public List<Book> getAllBooks() {
//        return bookList;
//    }
//
//    public List<Book> searchBook(String searchParam) {
//        // Comparator used to sort bookList
//        Comparator<Book> groupByComparator = Comparator.comparing(Book::getName).thenComparing(Book::getAuthor);
//        List<Book> result = bookList
//                .stream()
//                .filter(e -> e.getName().toLowerCase().contains(searchParam.toLowerCase()) || e.getAuthor().toLowerCase().contains(searchParam))
//                .sorted(groupByComparator)
//                .collect(Collectors.toList());
//
//        return result;
//    }
}
